git:
  hooks:
    pre-commit: ${repo.root}/blt/ddev/git-hooks
    pre-push: ${repo.root}/blt/ddev/git-hooks
    commit-msg: ${repo.root}/blt/ddev/git-hooks

command-hooks:
  frontend-reqs:
    dir: ${docroot}/themes/custom
    command: ''
  frontend-assets:
    dir: ${docroot}/themes/custom/wingsuit
    command: ''
