# Basic LAMP stack
version: "2.1"
services:
  # Web
  web:
    volumes:
      - ${WINGSUIT_PATH}:/var/www/docroot/themes/custom/wingsuit
  # CLI
  cli:
    volumes:
      - ${WINGSUIT_PATH}:/var/www/docroot/themes/custom/wingsuit
